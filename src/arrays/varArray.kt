package arrays

fun main(){

    var array = arrayOf('s',1,5,30.5f, "Hello")

    //Note: in var array, we can not change length of array
    //Note: in var array, we can change whole array to other array && as well each elements one by one

    //Array<Any> : any type of data types you can include inside array
    //Array<Datatype>

    println(array[2])
}